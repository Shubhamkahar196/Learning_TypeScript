// union

let subs: number | string = "1m"

let apiRequestStatus : "pending" | "success" | "error" = "pending"
apiRequestStatus = "success"


// any
let dynamicValue: any = 42;
dynamicValue = "Hello"; // OK, no error
dynamicValue = true;   // OK, no error


